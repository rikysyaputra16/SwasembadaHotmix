"use strict";
    function importWorkbookV115(buffer,fileName){importWorkbookV114(buffer,fileName);}
    function buildWorkbookV115(){const wb=buildWorkbookV114();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Bulan pada pengingat":"Template WhatsApp menampilkan Bulan Tagihan berdasarkan periode angsuran berikutnya.","Cetak laporan angsuran":"Cetak khusus Laporan Angsuran dibagi per 12 periode pada kertas A4 landscape; judul dan kolom ringkasan diulang pada setiap halaman."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:28},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Bulan Tagihan pada Pengingat":"Aktif - mengikuti periode angsuran berikutnya","Format Cetak Laporan Angsuran":"A4 landscape; 12 periode per halaman"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:40},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    function importWorkbookV116(buffer,fileName){importWorkbookV115(buffer,fileName);}
    function buildWorkbookV116(){const wb=buildWorkbookV115();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Tujuan pembayaran pengingat":"BANK BCA - rekening dari konfigurasi D1 a/n konfigurasi D1.","Peringatan tujuan transfer":"Pembayaran transfer hanya ditujukan kepada Bendahara RT yang saat ini menjabat, Bpk. konfigurasi D1; warga diminta tidak mengirim dana ke rekening atau pihak lain."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:30},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Rekening Resmi Pengingat":"BANK BCA - rekening dari konfigurasi D1 a/n konfigurasi D1","Bendahara RT pada Pengingat":"Bpk. konfigurasi D1","Peringatan Transfer":"Jangan mengirim dana ke rekening atau pihak lain"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:40},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    function importWorkbookV117(buffer,fileName){importWorkbookV116(buffer,fileName);state.members.forEach(member=>{member.tanggalAngsuran=installmentDate(member);});persistLocal();renderAll();}
    function buildWorkbookV117(){const wb=buildWorkbookV116(),reportMonth=monthKey(state.reportMonth)||currentMonth(),arrearsRows=arrearsForMonth(reportMonth).map(row=>({"Periode Laporan":reportMonth,"ID Anggota":row.member.id,"Nama":row.member.nama,"No. Rumah":row.member.noRumah,"No. HP":row.member.noHp,"Tanggal Angsuran":installmentDate(row.member),"Kewajiban s.d. Periode":row.expected,"Cicilan Dibayar s.d. Periode":row.paid,"Tunggakan":row.arrears,"Periode Tertunggak":row.overduePeriods,"Petugas":row.member.petugas||state.defaultOfficer||"Panitia"})),arrearsHeaders=["Periode Laporan","ID Anggota","Nama","No. Rumah","No. HP","Tanggal Angsuran","Kewajiban s.d. Periode","Cicilan Dibayar s.d. Periode","Tunggakan","Periode Tertunggak","Petugas"],wsArrears=XLSX.utils.json_to_sheet(arrearsRows,{header:arrearsHeaders});wsArrears["!autofilter"]={ref:`A1:K${Math.max(2,arrearsRows.length+1)}`};wsArrears["!cols"]=[{wch:18},{wch:14},{wch:30},{wch:11},{wch:24},{wch:19},{wch:24},{wch:28},{wch:18},{wch:20},{wch:18}];replaceSheet(wb,"Tunggakan",wsArrears);if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Pengingat pada daftar tunggakan":"Tombol Kirim Pengingat tersedia pada Daftar Tunggakan Bulan Berjalan.","Tanggal angsuran default":"Tanggal angsuran kosong otomatis menggunakan tanggal 10.","Pengingat multi-rumah":"Anggota aktif dengan nama dan nomor WhatsApp sama digabung dalam satu pengingat; nominal dasar, pembayaran, kewajiban, sisa, dan dana lebih dijumlahkan."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:32},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Pengingat pada Daftar Tunggakan":"Aktif","Tanggal Angsuran Default":"10","Aturan Pengingat Multi-rumah":"Gabungkan anggota aktif berdasarkan nama normalisasi dan nomor WhatsApp yang sama"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:42},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    function importWorkbookV118(buffer,fileName){importWorkbookV117(buffer,fileName);}
    function buildWorkbookV118(){const wb=buildWorkbookV117();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Rincian tunggakan pada pengingat":"Template WhatsApp menyebut seluruh nomor angsuran dan bulan yang masih tertunggak sampai periode laporan.","Total tagihan pada pengingat":"Jika tunggakan mencakup lebih dari satu bulan, tampilkan Total tagihan yang perlu dibayar tepat setelah Nominal angsuran dasar."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:34},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Rincian Tunggakan pada Pengingat":"Nomor angsuran, bulan, dan sisa nominal setiap tagihan","Kondisi Total Tagihan Pengingat":"Ditampilkan jika tunggakan mencakup lebih dari satu bulan"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:42},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    function importWorkbookV119(buffer,fileName){importWorkbookV118(buffer,fileName);}
    function buildWorkbookV119(){const wb=buildWorkbookV118();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Istilah tagihan pada pengingat":"Gunakan label Tagihan dan Rincian tagihan perlu dibayar.","Dana lebih pada pengingat":"Tidak ditampilkan pada pesan WhatsApp agar informasi kepada warga tidak ambigu; perhitungan internal tetap dipertahankan."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:34},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Label Tagihan Pengingat":"Tagihan","Label Rincian Pengingat":"Rincian tagihan perlu dibayar","Dana Lebih pada Pesan WhatsApp":"Tidak ditampilkan"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:42},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    function importWorkbookV120(buffer,fileName){importWorkbookV119(buffer,fileName);}
    function buildWorkbookV120(){const wb=buildWorkbookV119();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Scroll laporan ringkasan":"History Pembayaran Bulanan, Daftar Tunggakan Bulan Berjalan, dan Rincian Tunggakan Semua Bulan memakai area scroll pada layar; hasil cetak tetap menampilkan seluruh isi."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:34},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Scroll Laporan Ringkasan":"Aktif - tinggi maksimum 560 px; cetak tanpa pembatas"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:42},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
