"use strict";
    function importWorkbookV121(buffer,fileName){importWorkbookV120(buffer,fileName);}
    function buildWorkbookV121(){const wb=buildWorkbookV120();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Rincian tunggakan pada dashboard":"Tabel Rincian Tunggakan Semua Bulan tidak ditampilkan pada halaman Ringkasan. Data rinci tetap disimpan dalam workbook.","Format pesan WhatsApp":"Pesan diawali salam Arab. Nama, nomor rumah, periode, bulan, nominal, bank, nomor rekening, dan nama pemilik rekening memakai penanda tebal WhatsApp."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:36},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Rincian Tunggakan pada Dashboard":"Tidak ditampilkan","Salam Arab pada Pengingat":"Aktif","Format Tebal WhatsApp":"Nama; nomor rumah; angsuran; bulan; nominal; bank; nomor rekening; nama rekening"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:42},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    function importWorkbookV122(buffer,fileName){importWorkbookV121(buffer,fileName);}
    function buildWorkbookV122(){const wb=buildWorkbookV121();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Arah teks pengingat":"Salam Arab menggunakan arah kanan-ke-kiri. Setiap baris berbahasa Indonesia menggunakan penanda kiri-ke-kanan agar pesan WhatsApp tetap rata kiri."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:36},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Arah Salam Arab":"RTL","Arah Isi Pesan WhatsApp":"LTR per baris"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:42},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    function importWorkbookV123(buffer,fileName){importWorkbookV122(buffer,fileName);}
    function buildWorkbookV123(){const wb=buildWorkbookV122();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Kwitansi digital":"Setiap transaksi pada menu Pembayaran dapat dibuat menjadi kwitansi PNG vertikal 1080 x 1350 piksel, diunduh, atau dibagikan melalui menu berbagi perangkat/WhatsApp.","Isi kwitansi":"Kwitansi memuat status pencatatan, nominal, tanggal, ID transaksi, nama anggota, nomor rumah, kategori, periode, metode, penerima, dan catatan.","Batasan berbagi WhatsApp":"Jika browser tidak mendukung berbagi file langsung, aplikasi mengunduh PNG dan membuka chat WhatsApp agar pengguna melampirkan gambar secara manual."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:36},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Kwitansi Digital":"Aktif - PNG 1080 x 1350","Aksi Kwitansi":"Unduh PNG; bagikan file melalui perangkat; fallback WhatsApp Web","Sumber Data Kwitansi":"Transaksi Pembayaran dan Data Anggota"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:42},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    function importWorkbookV124(buffer,fileName){importWorkbookV123(buffer,fileName);}
    function buildWorkbookV124(){const wb=buildWorkbookV123();if(wb.Sheets.Panduan){const guide=XLSX.utils.sheet_to_json(wb.Sheets.Panduan,{header:1,defval:""}),rules={"Ringkasan kwitansi":"Kwitansi menampilkan Lama Angsuran dari Data Anggota serta Total Sudah Mengangsur, Total Kewajiban, dan Sisa Tagihan setelah transaksi tercatat.","WhatsApp kwitansi":"Tombol Bagikan / WhatsApp membuka chat langsung sesuai No. HP anggota dan mengunduh PNG untuk dilampirkan secara manual.","Transfer pembayaran":paymentDestinationText()?paymentDestinationText().replaceAll("*",""):"Tujuan pembayaran belum dikonfigurasi di D1.","Pembayaran tunai":state.cashCollectors||"Penerima tunai belum dikonfigurasi di D1."};Object.entries(rules).forEach(([key,value])=>{const row=guide.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else guide.push([key,value]);});const wsGuide=XLSX.utils.aoa_to_sheet(guide);wsGuide["!cols"]=[{wch:36},{wch:118}];replaceSheet(wb,"Panduan",wsGuide);}const metadata=XLSX.utils.sheet_to_json(wb.Sheets.Metadata,{header:1,defval:""}),metadataRules={"Versi Aplikasi":APP_VERSION,"Ringkasan Keuangan pada Kwitansi":"Total Sudah Mengangsur; Total Kewajiban; Sisa Tagihan","Lama Angsuran pada Kwitansi":"Diambil dari Data Anggota","Tujuan WhatsApp Kwitansi":"Chat langsung berdasarkan No. HP anggota; PNG diunduh untuk dilampirkan","Informasi Transfer":paymentDestinationText()?paymentDestinationText().replaceAll("*",""):"Konfigurasi D1","Penerima Default":state.defaultReceiver||"Bendahara","Penerima Tunai":state.cashCollectors||"Konfigurasi D1"};Object.entries(metadataRules).forEach(([key,value])=>{const row=metadata.find(item=>String(item[0]).trim()===key);if(row)row[1]=value;else metadata.push([key,value]);});const wsMetadata=XLSX.utils.aoa_to_sheet(metadata);wsMetadata["!cols"]=[{wch:42},{wch:118}];replaceSheet(wb,"Metadata",wsMetadata);sourceWorkbook=wb;return wb;}
    async function ensureWritePermission(request=true){if(!fileHandle)return false;const opts={mode:"readwrite"};if(!fileHandle.queryPermission||!fileHandle.requestPermission)return true;if((await fileHandle.queryPermission(opts))==="granted")return true;return request&&(await fileHandle.requestPermission(opts))==="granted";}
    async function loadExcelFromHandle(requestPermission=false){
      if(!fileHandle||!await ensureWritePermission(requestPermission))return false;
      const file=await fileHandle.getFile();importWorkbookV124(await file.arrayBuffer(),file.name);setConnection(`Tersambung otomatis: ${file.name}`,true);return true;
    }
    async function restoreExcelHandle(){
      try{const restored=await proofDbValue(PROOF_SETTINGS_STORE,EXCEL_FILE_KEY);if(!fileHandle)fileHandle=restored;if(fileHandle)await loadExcelFromHandle(false);}catch(_e){excelStorageReady=false;refreshAutomaticStorageUi();}
    }
    async function saveExcel(automatic=true){
      if(automatic){await saveSettingsRemote();return;}
      if(!window.XLSX)throw new Error("Pustaka Excel tidak tersedia.");
      const wb=buildWorkbookV124();
      XLSX.writeFile(wb,state.fileName||"Swasembada_Hotmix_RT_Atas.xlsx",{cellDates:true});
      toast("Export Excel berhasil dibuat dari data Cloudflare D1.");
    }
    async function connectExcel(){
      return saveExcel(false);
    }
    async function initializeAutomaticStorage(){
      try{
        cloudReady=false;refreshAutomaticStorageUi();
        const payload=await apiJson("/api/bootstrap");
        const remote=payload?.state;if(!remote?.members?.length)throw new Error("Data D1 kosong. Jalankan migration terlebih dahulu.");
        state={...remote,plans:normalizePlanMonths(remote.plans||[]),payments:(remote.payments||[]).map(payment=>({...payment,category:payment.category==="DP Awal"?"Custom Pembayaran":payment.category||"Cicilan"}))};
        state.reportMonth=rollingReportMonth(state.reportMonth);state.historyMonth=monthKey(state.historyMonth)||currentMonth();state.historyMember=String(state.historyMember||"");
        state.members.forEach(member=>{member.planType=member.planType||defaultPlanType(member);member.tanggalAngsuran=installmentDate(member);assignAutomaticPeriods(member.id);});
        fileHandle={cloud:true};cloudReady=true;proofStorageReady=true;excelStorageReady=true;persistLocal();renderAll();renderPaymentConfig();setProofFolderStatus();refreshAutomaticStorageUi();
        return true;
      }catch(error){
        fileHandle=null;cloudReady=false;proofStorageReady=false;excelStorageReady=false;refreshAutomaticStorageUi();renderAll();toast(`Cloudflare belum siap: ${error.message}`,true);return false;
      }
    }
